import java.util.*;

public class AlphaMod5 {

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        // 1.In program input 3 number:A,B,C. you have to output of three number.
        // System.out.println("Enter Three number:");
        // int a = sc.nextInt();
        // int b = sc.nextInt();
        // int c = sc.nextInt();
        // int avg = (a + b + c) / 3;
        // System.out.println("the avg is:" + avg);

        // 3.In a program,input the side of a square.You have to output the area of the
        // square.

        // System.out.println("Enter the side of squre:");
        // int side = sc.nextInt();
        // int area = side * side;
        // System.out.println("The sqare area is:" + area);

        // 4. Enter cost of 3 items from the user (using float datatype) -a pencil,a pen
        // and an eraser.
        // You have to output the total cost of the items back to the user as their
        // bill.

        // System.out.println("Enter pencil,penand Eraser price:");
        // float pencil = sc.nextFloat();
        // float pen = sc.nextFloat();
        // float eraser = sc.nextFloat();

        // float bill = pencil + pen + eraser;
        // System.out.println("Your bill is: " + bill);

        // NOTE:--> name of variable start with any alphabet , underscore(_) or
        // dollor($)
    }
}
